export enum CreditCardDonationStatus {
  PENDING,
  PROCESSING,
  FINISHED,
  FAILED_RETRY,
  FAILED
};

export enum BitcoinDonationStatus {
  PENDING,
  PROCESSING,
  FINISHED,
  FAILED_RETRY,
  FAILED
};

export enum EthereumDonationStatus {
  FINISHED
};